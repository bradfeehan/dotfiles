# Get editor completions based on the config schema
"$schema" = 'https://starship.rs/config-schema.json'

add_newline = false

format = """
$username\
$os\
$hostname\
$localip\
$shlvl\
$kubernetes\
$directory\
$jobs\
$battery\
$status\
$container\
$netns\
$shell\
$character"""

right_format = """
${custom.git_branch}\
${custom.git_commit}\
${custom.git_metrics}\
${custom.git_status}\
${custom.jj}\
$all"""

[directory]
fish_style_pwd_dir_length = 1
style = "fg:31"
repo_root_style = "fg:39 bold"
before_repo_root_style = "fg:103"

# JJ custom

# custom module for jj status
[custom.jj]
description = "The current jj status"
when = '''bash --norc --noprofile -c 'd="${PWD}"; while [[ -n "$d" ]]; do [[ -d "$d/.jj" ]] && exit 0; [[ "$d" == "/" ]] && break; d="${d%/*}"; [[ -z "$d" ]] && d="/"; done; exit 1' '''
symbol = "🥋 "
command = '''
jj log --revisions @ --no-graph --ignore-working-copy --color always --limit 1 --template '
  separate(" ",
    change_id.shortest(4),
    bookmarks,
    "|",
    concat(
      if(conflict, "💥"),
      if(divergent, "🚧"),
      if(hidden, "👻"),
      if(immutable, "🔒"),
    ),
    raw_escape_sequence("\x1b[1;32m") ++ if(empty, "(empty)"),
    raw_escape_sequence("\x1b[1;32m") ++ coalesce(
      truncate_end(29, description.first_line(), "…"),
      if(empty,
        "(none)",
        raw_escape_sequence("\x1b[1;33m") ++ "(none)"
      )
    ) ++ raw_escape_sequence("\x1b[0m"),
  )
'
'''

# optionally disable git modules when JJ is found.
# note that you'll need to add ${custom.git_branch}, ${custom.git_commit} etc
# into format: https://starship.rs/config/#default-prompt-format
[git_status]
disabled = true

[custom.git_status]
when = '''! bash --norc --noprofile -c 'd="${PWD}"; while [[ -n "$d" ]]; do [[ -d "$d/.jj" ]] && exit 0; [[ "$d" == "/" ]] && break; d="${d%/*}"; [[ -z "$d" ]] && d="/"; done; exit 1' '''
command = "starship module git_status"
style = "" # This disables the default "(bold green)" style
description = "Only show git_status if we're not in a jj repo"

[git_commit]
disabled = true
tag_symbol = '  '

[custom.git_commit]
when = '''! bash --norc --noprofile -c 'd="${PWD}"; while [[ -n "$d" ]]; do [[ -d "$d/.jj" ]] && exit 0; [[ "$d" == "/" ]] && break; d="${d%/*}"; [[ -z "$d" ]] && d="/"; done; exit 1' '''
command = "starship module git_commit"
style = ""
description = "Only show git_commit if we're not in a jj repo"

[git_metrics]
disabled = true

[custom.git_metrics]
when = '''! bash --norc --noprofile -c 'd="${PWD}"; while [[ -n "$d" ]]; do [[ -d "$d/.jj" ]] && exit 0; [[ "$d" == "/" ]] && break; d="${d%/*}"; [[ -z "$d" ]] && d="/"; done; exit 1' '''
command = "starship module git_metrics"
description = "Only show git_metrics if we're not in a jj repo"
style = ""

[git_branch]
disabled = true

[custom.git_branch]
when = '''! bash --norc --noprofile -c 'd="${PWD}"; while [[ -n "$d" ]]; do [[ -d "$d/.jj" ]] && exit 0; [[ "$d" == "/" ]] && break; d="${d%/*}"; [[ -z "$d" ]] && d="/"; done; exit 1' '''
command = "starship module git_branch"
description = "Only show git_branch if we're not in a jj repo"
style = ""



# Symbols
[c]
symbol = " "

[cmd_duration]
format = '[ $duration]($style) '
style = "fg:101"

[cpp]
symbol = " "

[cmake]
symbol = " "

[crystal]
symbol = " "

[docker_context]
symbol = " "

[elixir]
symbol = " "

[golang]
symbol = " "

[haskell]
symbol = " "

[hostname]
ssh_symbol = " "

[java]
symbol = " "

[kotlin]
symbol = " "

[memory_usage]
symbol = "󰍛 "

[nim]
symbol = "󰆥 "

[nodejs]
symbol = " "

[os]
disabled = false

[os.symbols]
Alpaquita = " "
Alpine = " "
AlmaLinux = " "
Amazon = " "
Android = " "
AOSC = " "
Arch = " "
Artix = " "
CachyOS = " "
CentOS = " "
Debian = " "
DragonFly = " "
Emscripten = " "
EndeavourOS = " "
Fedora = " "
FreeBSD = " "
Garuda = "󰛓 "
Gentoo = " "
HardenedBSD = "󰞌 "
Illumos = "󰈸 "
Kali = " "
Linux = " "
Mabox = " "
Macos = " "
Manjaro = " "
Mariner = " "
MidnightBSD = " "
Mint = " "
NetBSD = " "
NixOS = " "
Nobara = " "
OpenBSD = "󰈺 "
openSUSE = " "
OracleLinux = "󰌷 "
Pop = " "
Raspbian = " "
Redhat = " "
RedHatEnterprise = " "
RockyLinux = " "
Redox = "󰀘 "
Solus = "󰠳 "
SUSE = " "
Ubuntu = " "
Unknown = " "
Void = " "
Windows = "󰍲 "

[python]
symbol = " "

[ruby]
symbol = " "

[rust]
symbol = "󱘗 "

[status]
symbol = " "

[swift]
symbol = " "

[time]
disabled = false
format = '[ $time]($style) '
style = "fg:66"


# Disabled default modules

[package]
disabled = true

[singularity]
disabled = true

[vcsh]
disabled = true

[fossil_branch]
disabled = true

[fossil_metrics]
disabled = true

[hg_branch]
disabled = true

[hg_state]
disabled = true

[cobol]
disabled = true

[daml]
disabled = true

[deno]
disabled = true

[dotnet]
disabled = true

[elm]
disabled = true

[fennel]
disabled = true

[fortran]
disabled = true

[guix_shell]
disabled = true

[haxe]
disabled = true

[julia]
disabled = true

[opa]
disabled = true

[perl]
disabled = true

[php]
disabled = true

[pulumi]
disabled = true

[purescript]
disabled = true

[quarto]
disabled = true

[raku]
disabled = true

[rlang]
disabled = true

[red]
disabled = true

[scala]
disabled = true

[solidity]
disabled = true

[typst]
disabled = true

[vlang]
disabled = true

[vagrant]
disabled = true

[buf]
disabled = true

[nix_shell]
disabled = true

[meson]
disabled = true

[spack]
disabled = true

[openstack]
disabled = true

[nats]
disabled = true
