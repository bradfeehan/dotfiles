# only if "${HOME}/.orbstack/shell/init.zsh" exists
if [[ -e "${HOME}/.orbstack/shell/init.zsh" ]]; then
    # Added by OrbStack: command-line tools and integration
    . "${HOME}/.orbstack/shell/init.zsh"
fi
